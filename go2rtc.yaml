app:
  modules: [api, rtsp, webrtc, exec, ffmpeg, mjpeg]

api:
  listen: ":1984"
  allow_paths: ["/api", "/api/streams", "/api/webrtc", "/api/frame.jpeg"]

rtsp:
  listen: ""

exec:
  allow_paths: [ffmpeg]

webrtc:
  listen: ":8555"

streams:

log:
  level: info
