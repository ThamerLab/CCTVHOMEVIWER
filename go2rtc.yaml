app:
  modules: [api, ws, rtsp, webrtc, exec, ffmpeg, mjpeg]

api:
  listen: ":1984"
  allow_paths: ["/", "/api/streams", "/api/ws"]

rtsp:
  listen: ""

exec:
  allow_paths: [ffmpeg]

webrtc:
  listen: ":8555"

streams:

log:
  level: info
