app:
  modules: [api, ws, rtsp, webrtc, exec, ffmpeg, mjpeg, homekit]

api:
  listen: ":1984"
  allow_paths: ["/", "/api/streams", "/api/ws", "/api/homekit"]

rtsp:
  listen: ""

exec:
  allow_paths: [ffmpeg]

webrtc:
  listen: ":8555"

streams:

homekit:
  # Enable HomeKit per camera from the admin page.

log:
  level: info
